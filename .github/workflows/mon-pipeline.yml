name: Pipeline Site HTML

on:
  push:
    branches: [ "main" ]
    workflow-dispatch:

jobs:

  verification:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Vérifier la présence du fichier index
        run: |
          if [ -f "index.html" ]; then
            echo "🟢 Le fichier index.html a été trouvé !"
          else
            echo "🔴 ERREUR : index.html est introuvable à la racine du projet."
            exit 1
          fi


  deploy:
    runs-on: ubuntu-latest
    needs: verification
    environment: production
    steps:
      - name: Déploiement
        run: echo "🚀 Votre site HTML est en cours de déploiement sur le serveur !"